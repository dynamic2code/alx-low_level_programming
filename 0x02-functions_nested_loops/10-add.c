#include "main.h"
/**
 *add - a function that adds two intagers.
 *
 *returns the result.
 *
 *@x : first intager
 *@y : second intager
 *Return: Add of number one and number two.
 */
int add(int x, int y)
{
int sum;
sum = x + y;
return (sum);
}
